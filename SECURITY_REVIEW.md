# 安全审查

审查日期：2026 年 8 月 10 日

## 范围

本次审查覆盖：

- 当前 Git 跟踪文件与工作区文件
- Git 提交历史中的文件名和补丁内容
- Swift 源码中的网络、凭据和敏感数据模式
- Xcode 签名、沙盒和平台配置
- 联系人导入、导出、合并和删除流程

## 审查结果

未发现需要阻止发布的高危问题。

### 数据与凭据

- 未发现导出的联系人备份或真实联系人测试数据。
- 未发现私钥、证书、Provisioning Profile、密码、API 密钥或访问令牌。
- Xcode 工程不保存 Development Team ID，发布者需要在本地配置签名团队。
- `xcuserdata`、DerivedData、联系人备份和常见签名材料已加入 `.gitignore`。

### 网络与隐私

- Swift 源码未使用 `URLSession`、Network.framework 或第三方网络库。
- macOS App Sandbox 的出站网络权限已关闭。
- 应用不包含分析、广告、遥测或第三方追踪 SDK。
- 联系人数据仅通过系统 Contacts 框架访问。

### 文件导入

- 导入前限制备份文件最大为 100 MB。
- JSON 文件必须通过格式版本、结构和联系人数量校验。
- 导入只新增缺失联系人或补回缺失头像，不删除和覆盖已有联系人。
- 文件选择器可以选择任意系统文件类型，但非有效备份会在写入前被拒绝。

### 破坏性操作

- 单组合并、批量合并和删除全部联系人均要求明确确认。
- 批量合并先构造一个 `CNSaveRequest`，再统一提交，降低部分完成的风险。
- 批量合并失败后会重新读取通讯录，避免界面报告错误状态。
- 查重依据会在列表、详情和合并确认中展示。
- 同名 List 只允许在同一 Contacts 容器内合并；成员汇总后才删除重复 List，联系人本身不会被删除。

## 剩余风险

1. JSON 备份包含敏感联系人数据且未加密。用户必须自行保护备份文件。
2. 相同姓名和链式匹配可能产生误判。用户应在合并前检查判定依据。
3. 系统通讯录账号可能将修改同步到 iCloud、Google 或其他服务，行为由用户的系统账号设置决定。
4. 项目目前没有独立 XCTest 测试目标，关键流程主要通过构建、内存往返测试和人工验证覆盖。
5. Contacts 框架写入属于高影响操作，正式发布前仍应使用专门的测试通讯录完成端到端回归。

## 发布前建议

- 使用有效 Apple Development 或 Distribution 证书构建发布版本。
- 在干净测试账号中验证授权、备份、恢复、单组合并、批量合并和删除流程。
- 为备份编解码、重复分组和保留联系人评分增加自动化单元测试。
- 未来增加网络、云同步或分析功能时，重新进行安全审查并更新隐私说明。
